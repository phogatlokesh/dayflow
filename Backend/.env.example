# Database Configuration
PGUSER=postgres
PGPASSWORD=Abhishek
PGHOST=localhost
PGPORT=5433
PGDATABASE=dayflow

# Server Configuration
PORT=5000
NODE_ENV=development

# For development only - set to your machine's IP if accessing from other machines
# VITE_API_URL=http://<your-machine-ip>:5000
