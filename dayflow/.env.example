# API Configuration
# Use this when accessing from a different machine on the same network
# Replace <your-backend-ip> with the IP address of the machine running the backend
VITE_API_URL=http://localhost:5000

# For accessing from another machine, uncomment and update:
# VITE_API_URL=http://<your-backend-machine-ip>:5000
